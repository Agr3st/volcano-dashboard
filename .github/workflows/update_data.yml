name: Update Volcano Activity CSV

on:
  schedule:
    - cron: '55 16 * * *'  # codziennie o 6:00 UTC
  workflow_dispatch:      # pozwala też uruchomić ręcznie z GitHub UI

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12.9"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: |
          python scraper/scrape_current_eruptions.py

      - name: Commit and push if data changed
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/volcano_activity.csv
          git diff --cached --quiet || git commit -m "Auto-update volcano_activity.csv"
          git push
